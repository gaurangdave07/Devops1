global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'nodejs-app'
    static_configs:
      # Replace 'host.docker.internal' with your application's public IP if running on separate servers
      - targets: ['host.docker.internal:3000']
